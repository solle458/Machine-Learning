# Math and theory（理論・数学（ノート・演習））

`math_theory` の実験用ディレクトリ。

## データ

ローカルの `data/` に置く（Git には上げない）。入手元と前処理メモは [data/README.md](data/README.md) に書く。

## 実行例

リポジトリルートで（`uv` 利用時の例）:

```bash
cd topics/math_theory
# uv run python src/main.py   # 例
```
